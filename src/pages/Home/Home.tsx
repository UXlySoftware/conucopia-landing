import React, { Fragment } from "react";
import Hero from "@coinucopia/sections/Hero";

const Home: React.FC = () => {
  return (
    <Fragment>
      <Hero />
    </Fragment>
  );
};

export default Home;
